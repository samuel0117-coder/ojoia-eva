from fastapi import FastAPI
app = FastAPI()

@app.get("/health")
async def health():
    return {"status": "ok", "test": True}

@app.post("/api/chat/eva")
async def test_chat():
    return {"success": True, "message": "chat works"}
